// This is here so I can fix the G15 issues
Class ONSPlusHUDOns extends ONSPlusHUD;

/*simulated function PostRender(canvas Canvas)
{
	Super.PostRender(Canvas);

	if (bShowTeamActionPlan)
		ONSPlusDrawActionPlan(Canvas);
}*/