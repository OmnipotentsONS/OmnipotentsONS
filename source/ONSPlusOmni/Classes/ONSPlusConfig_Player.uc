// Used to hold config data which will be independant of the package name
Class ONSPlusConfig_Player extends Object
	PerObjectConfig
	config(ONSPlus);

var config bool bDisableExitPointDisplay;
var config bool bDisableSelectableExits;
var config bool bDisableEnhancedRadarMap;